name: Deploy

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:    
    
      - name: checkout
        uses: actions/checkout@v3

      - name: setup SSH
      - uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_KEY }}

      - name: create file
        run: ssh azureuser@20.55.28.37 touch hola.txt


